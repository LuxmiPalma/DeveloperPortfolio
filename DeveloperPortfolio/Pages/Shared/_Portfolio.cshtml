@model List<DeveloperPortfolio.Models.DTO.ProjectDTO>

<div class="container">
    <div class="row gy-4 portfolio-container" data-aos="fade-up" data-aos-delay="200">

        @foreach (var project in Model)
        {
            <div class="col-lg-4 col-md-6 portfolio-item">
                <div class="card h-100 shadow-sm">
                    <img src="@project.ProjectImg" class="card-img-top" />

                    <div class="card-body">
                        <h5 class="card-title">@project.Name</h5>
                        <p class="card-text">@project.Description</p>

                        <div class="mb-2">
                            <strong>Technologies:</strong>
                            <div class="d-flex flex-wrap mt-1">
                                @foreach (var tech in project.Technologies)
                                {
                                    <img src="@tech.Url" title="@tech.Technology"
                                         alt="@tech.Technology"
                                         class="tech-icon"
                                         style="height: 24px; margin-right: 5px;" />




                                }
                            </div>
                        </div>
                    </div>

                    <div class="card-footer text-center">
                        @if (!string.IsNullOrEmpty(project.GitHubUrl))
                        {
                            <a href="@project.GitHubUrl" target="_blank" class="btn btn-outline-dark btn-sm me-1">
                                <i class="bi bi-github"></i> GitHub
                            </a>
                        }
                        @if (!string.IsNullOrEmpty(project.LiveDemoUrl))
                        {
                            <a href="@project.LiveDemoUrl" target="_blank" class="btn btn-outline-primary btn-sm">
                                <i class="bi bi-box-arrow-up-right"></i> Live Demo
                            </a>
                        }
                    </div>
                </div>
            </div>
        }
    </div>
</div>